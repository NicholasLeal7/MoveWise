<!DOCTYPE html>
<html lang="pt-br">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Move Wise - Descubra Seu Próximo Destino</title>

  <!-- Bootstrap 5 -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />

  <!-- Bootstrap Icons -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" rel="stylesheet" />

  <!-- AOS - Animate On Scroll -->
  <link href="https://unpkg.com/aos@2.3.1/dist/aos.css" rel="stylesheet">

  <style>
    :root {
      --primary-color: #0d6efd;
      --primary-light: rgba(13, 110, 253, 0.1);
      --text-color: #2c3e50;
    }

    body {
      background: linear-gradient(135deg, var(--primary-light) 0%, #ffffff 100%);
      min-height: 100vh;
      overflow-x: hidden;
      color: var(--text-color);
    }

    .page-container {
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 2rem;
      position: relative;
    }

    .content-wrapper {
      background: rgba(255, 255, 255, 0.9);
      backdrop-filter: blur(10px);
      border-radius: 2rem;
      overflow: hidden;
      box-shadow: 0 20px 40px rgba(0, 0, 0, 0.1);
      width: 100%;
      max-width: 1200px;
      position: relative;
      z-index: 1;
    }

    .background-elements {
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      pointer-events: none;
      z-index: 0;
    }

    .floating-icon {
      position: absolute;
      font-size: 3rem;
      color: var(--primary-color);
      opacity: 0.1;
      animation: float 6s infinite;
    }

    .icon-1 { top: 10%; left: 10%; animation-delay: 0s; }
    .icon-2 { top: 20%; right: 20%; animation-delay: 1s; }
    .icon-3 { bottom: 15%; left: 15%; animation-delay: 2s; }
    .icon-4 { bottom: 25%; right: 10%; animation-delay: 3s; }

    @keyframes float {
      0%, 100% { transform: translate(0, 0) rotate(0deg); }
      25% { transform: translate(10px, -10px) rotate(5deg); }
      50% { transform: translate(0, -20px) rotate(0deg); }
      75% { transform: translate(-10px, -10px) rotate(-5deg); }
    }

    .login-section {
      padding: 3rem;
      background: white;
      border-radius: 2rem;
    }

    .features-section {
      padding: 3rem;
      background: linear-gradient(135deg, #ffffff 0%, var(--primary-light) 100%);
      border-radius: 2rem;
    }

    .logo-container {
      margin-bottom: 2rem;
      position: relative;
    }

    .logo-login {
      width: 140px;
      height: auto;
      object-fit: contain;
      transition: transform 0.3s ease;
      position: relative;
      z-index: 1;
    }

    .logo-glow {
      position: absolute;
      top: 50%;
      left: 50%;
      transform: translate(-50%, -50%);
      width: 160px;
      height: 160px;
      background: radial-gradient(circle, var(--primary-light) 0%, rgba(255,255,255,0) 70%);
      animation: glow 3s infinite;
    }

    @keyframes glow {
      0%, 100% { transform: translate(-50%, -50%) scale(1); opacity: 0.5; }
      50% { transform: translate(-50%, -50%) scale(1.1); opacity: 0.8; }
    }

    .form-control {
      border: 2px solid #e9ecef;
      border-radius: 1rem;
      padding: 0.8rem 1.2rem;
      transition: all 0.3s ease;
      background: rgba(255, 255, 255, 0.9);
    }

    .form-control:focus {
      border-color: var(--primary-color);
      box-shadow: 0 0 0 0.25rem var(--primary-light);
    }

    .btn {
      border-radius: 1rem;
      padding: 0.8rem 1.5rem;
      transition: all 0.3s ease;
    }

    .btn-primary {
      background: linear-gradient(135deg, var(--primary-color) 0%, #0099ff 100%);
      border: none;
      position: relative;
      overflow: hidden;
    }

    .btn-primary::after {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      width: 200%;
      height: 100%;
      background: linear-gradient(90deg, transparent, rgba(255,255,255,0.2), transparent);
      transform: translateX(-100%);
    }

    .btn-primary:hover::after {
      animation: shine 1.5s infinite;
    }

    @keyframes shine {
      to { transform: translateX(50%); }
    }

    .feature-card {
      background: rgba(255, 255, 255, 0.9);
      border-radius: 1.5rem;
      padding: 1.5rem;
      margin-bottom: 1.5rem;
      transition: all 0.3s ease;
      border: 2px solid transparent;
    }

    .feature-card:hover {
      transform: translateY(-5px);
      border-color: var(--primary-color);
      box-shadow: 0 10px 20px rgba(13, 110, 253, 0.1);
    }

    .feature-icon {
      font-size: 2.5rem;
      background: linear-gradient(135deg, var(--primary-color) 0%, #0099ff 100%);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      margin-bottom: 1rem;
    }

    .disclaimer {
      font-size: 0.85rem;
      color: #6c757d;
      padding: 1rem;
      border-radius: 1rem;
      background: var(--primary-light);
      margin-top: 1.5rem;
    }

    .sources-badge {
      display: inline-block;
      padding: 0.3rem 0.8rem;
      background: rgba(13, 110, 253, 0.1);
      border-radius: 2rem;
      font-size: 0.8rem;
      color: var(--primary-color);
      margin: 0.2rem;
    }

    @media (max-width: 991.98px) {
      .page-container {
        padding: 1rem;
      }
      
      .content-wrapper {
        border-radius: 1.5rem;
      }

      .login-section, .features-section {
        padding: 2rem;
      }
    }
  </style>

</head>

<body>
  <!-- Background Elements -->
  <div class="background-elements">
    <i class="bi bi-globe-americas floating-icon icon-1"></i>
    <i class="bi bi-briefcase floating-icon icon-2"></i>
    <i class="bi bi-graph-up floating-icon icon-3"></i>
    <i class="bi bi-house-heart floating-icon icon-4"></i>
  </div>

  <div class="page-container">
    <div class="content-wrapper">
      <div class="row g-0">
        <div class="col-lg-5">
          <div class="login-section" data-aos="fade-right">
            <div class="logo-container text-center">
              <div class="logo-glow"></div>
              <img src="media/logo.jpg" alt="Logo Move Wise" class="logo-login">
            </div>

            <h3 class="text-center fw-bold mb-2">Bem-vindo ao Move Wise</h3>
            <p class="text-center text-muted mb-4">Descubra o melhor destino para sua carreira</p>

            <form id="loginForm" method="POST" action="/login">
              <div class="mb-3">
                <label for="usuario" class="form-label">
                  <i class="bi bi-person-circle text-primary"></i> Usuário
                </label>
                <input type="text" class="form-control" id="usuario" name="username" placeholder="Digite seu usuário" required />
              </div>

              <div class="mb-4">
                <label for="senha" class="form-label">
                  <i class="bi bi-lock-fill text-primary"></i> Senha
                </label>
                <input type="password" class="form-control" name="password" id="senha" placeholder="Digite sua senha" required />
              </div>

              <button type="submit" class="btn btn-primary w-100 mb-3">
                Entrar <i class="bi bi-box-arrow-in-right ms-1"></i>
              </button>

              <a class="btn btn-outline-secondary w-100" href="register">
                <i class="bi bi-person-plus-fill me-1"></i> Criar Conta
              </a>

              <div class="disclaimer text-center">
                <i class="bi bi-info-circle me-1"></i>
                Plataforma informativa baseada em:
                <div class="mt-2">
                  <span class="sources-badge"><i class="bi bi-graph-up me-1"></i>PayScale</span>
                  <span class="sources-badge"><i class="bi bi-house-heart me-1"></i>Numbeo</span>
                </div>
              </div>
            </form>
          </div>
        </div>

        <div class="col-lg-7">
          <div class="features-section" data-aos="fade-left">
            <div class="mb-5">
              <h2 class="fw-bold text-primary mb-3">Explore Novas Oportunidades</h2>
              <p class="lead">Análise inteligente de salários e custo de vida em diferentes países para sua profissão.</p>
            </div>

            <div class="row">
              <div class="col-md-6" data-aos="fade-up" data-aos-delay="100">
                <div class="feature-card">
                  <i class="bi bi-graph-up feature-icon"></i>
                  <h4>Análise Salarial</h4>
                  <p>Compare salários globais na sua área e descubra onde sua profissão é mais valorizada.</p>
                </div>
              </div>

              <div class="col-md-6" data-aos="fade-up" data-aos-delay="200">
                <div class="feature-card">
                  <i class="bi bi-house-heart feature-icon"></i>
                  <h4>Custo de Vida</h4>
                  <p>Avalie gastos com moradia, alimentação, transporte e lazer em diferentes cidades.</p>
                </div>
              </div>

              <div class="col-md-6" data-aos="fade-up" data-aos-delay="300">
                <div class="feature-card">
                  <i class="bi bi-shield-check feature-icon"></i>
                  <h4>Qualidade de Vida</h4>
                  <p>Índices de segurança, saúde e bem-estar para uma decisão mais consciente.</p>
                </div>
              </div>

              <div class="col-md-6" data-aos="fade-up" data-aos-delay="400">
                <div class="feature-card">
                  <i class="bi bi-sun feature-icon"></i>
                  <h4>Clima e Cultura</h4>
                  <p>Informações sobre clima, população e aspectos culturais para melhor adaptação.</p>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- Scripts -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
  <script src="https://unpkg.com/aos@2.3.1/dist/aos.js"></script>
  <script>
    AOS.init({
      duration: 1000,
      once: true
    });

    const formLogin = document.querySelector('#loginForm');
    
    formLogin.addEventListener("submit", async (evt) => {
      evt.preventDefault();
      const form = new FormData(formLogin);
      const data = {};

      form.forEach((value, key) => {
        data[key] = value;
      });

      try {
        const response = await fetch('/login', {
          method: 'POST',
          headers: {
            'Content-Type': 'application/json',
          },
          body: JSON.stringify(data)
        });

        const result = await response.json();

        if (!result.error && result.token) {
          const userInfo = {
            token: result.token,
            username: result.user.username,
            name: result.user.name
          };
          localStorage.setItem('user-info', JSON.stringify(userInfo));
          window.location.href = '/';
        } else {
          Swal.fire({
            icon: 'error',
            title: 'Falha no Login',
            text: 'Usuário ou senha inválidos. Verifique os dados e tente novamente.',
            confirmButtonText: 'Ok',
            confirmButtonColor: '#d33'
          });
        }
      } catch (error) {
        Swal.fire({
          icon: 'error',
          title: 'Erro',
          text: 'Ocorreu um erro ao tentar fazer login. Tente novamente mais tarde.',
          confirmButtonText: 'Ok',
          confirmButtonColor: '#d33'
        });
      }
    });
  </script>
</body>

</html>