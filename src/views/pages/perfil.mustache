<!DOCTYPE html>
<html lang="pt-br">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Perfil do Usuário</title>

  <!-- Bootstrap 5 -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" rel="stylesheet">

  <!-- Select2 CSS -->
  <link href="https://cdn.jsdelivr.net/npm/select2@4.1.0-rc.0/dist/css/select2.min.css" rel="stylesheet" />
</head>

<body class="bg-light py-5">
  <div class="container my-5">
    <div class="card shadow-lg border-0 p-4 rounded-4 mx-auto" style="max-width: 1000px;">
      <h3 class="mb-2 text-center fw-bold">🎯 Olá {{name}}, personalize seu perfil</h3>
      <p class="text-center text-muted mb-4">Nos ajude a entender sua realidade e encontrar os melhores destinos para
        você!</p>
      <hr>

      <form method="POST" action="../{{username}}">
        <div class="row g-4">

          <!-- Renda e Custo de Vida -->
          <div class="col-md-6">
            <label for="rendaMensal" class="form-label">
              <i class="bi bi-wallet2 me-1 text-primary"></i> Sua Renda Mensal
            </label>
            <div class="input-group">
              <span class="input-group-text">R$</span>
              <input type="text" class="form-control money" id="rendaMensal" name="salary" placeholder="0000" value="{{salary}}">
            </div>
          </div>

          <div class="col-md-6">
            <label for="custoVida" class="form-label">
              <i class="bi bi-house-heart-fill me-1 text-danger"></i> Custo de Vida Atual
            </label>
            <div class="input-group">
              <span class="input-group-text">R$</span>
              <input type="text" class="form-control money" id="custoVida" name="costOfLiving" placeholder="0000" value="{{costOfLiving}}">
            </div>
          </div>

          <!-- Profissão e País -->
          <div class="col-md-6">
            <label for="profissao" class="form-label">
              <i class="bi bi-person-workspace me-1 text-info"></i> Profissão
            </label>
            <select class="form-select select2" id="profissao" name="profession" data-placeholder="Selecione sua profissão">
              <option></option>
              {{{professions}}}
            </select>
          </div>

          <div class="col-md-6">
            <label for="paisOrigem" class="form-label">
              <i class="bi bi-geo-alt me-1 text-warning"></i> País de Origem
            </label>
            <select class="form-select select2" id="paisOrigem" name="originCountry" data-placeholder="Selecione seu país de origem">
              <option></option>
              {{{countries}}}
            </select>
          </div>

          <!-- Continente e Idiomas -->
          <div class="col-md-6">
            <label for="continentes" class="form-label">
              <i class="bi bi-globe-americas me-1 text-success"></i> Continentes de Interesse
            </label>
            <select class="form-select select2" id="continentes" name="favoriteContinent" multiple data-placeholder="Selecione continentes preferidos">
              <option></option>
              {{{subregions}}}
            </select>
          </div>

          <div class="col-md-6">
            <label for="idiomas" class="form-label">
              <i class="bi bi-translate me-1 text-secondary"></i> Idiomas que você fala
            </label>
            <select class="form-select select2" id="idiomas" name="languages" multiple data-placeholder="Selecione os idiomas">
              <option></option>
              {{{languages}}}
            </select>
          </div>

          <!-- Expectativa Salarial -->
          <div class="col-md-6">
            <label for="expectativaSalarial" class="form-label">
              <i class="bi bi-graph-up-arrow me-1 text-success"></i> Expectativa Salarial
            </label>
            <div class="input-group">
              <span class="input-group-text">R$</span>
              <input type="text" class="form-control money" id="expectativaSalarial" name="salaryExpect" placeholder="0000" value="{{salaryExpect}}">
            </div>
          </div>

        </div>

        <!-- Botão -->
        <div class="text-center mt-5">
          <button type="submit" class="btn btn-primary px-5 py-2">
            Salvar Perfil <i class="bi bi-check-circle ms-2"></i>
          </button>
        </div>
      </form>
    </div>
  </div>



  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

  <!-- jQuery -->
  <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>

  <!-- Select2 JS -->
  <script src="https://cdn.jsdelivr.net/npm/select2@4.1.0-rc.0/dist/js/select2.min.js"></script>

  <!-- jQuery Mask Plugin -->
  <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery.mask/1.14.16/jquery.mask.min.js"></script>

  <!-- SweetAlert2 CSS + JS -->
  <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>



  <!-- Inicialização dos plugins -->
  <script>
 $(document).ready(async function () {

      $('.select2').each(function () {
        $(this).select2({
          placeholder: $(this).data('placeholder') || "Selecione",
          allowClear: true,
          width: '100%'
        });
      });

      $('.money').mask('000.000.000', { reverse: true });

    });
  </script>
</body>


</html>